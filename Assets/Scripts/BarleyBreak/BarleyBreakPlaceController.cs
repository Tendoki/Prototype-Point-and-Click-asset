using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BarleyBreakPlaceController : MonoBehaviour
{
    public GameObject RightPlace;
    public GameObject LeftPlace;
    public GameObject UpPlace;
    public GameObject DownPlace;
    public bool isFilled;
    public int id;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
